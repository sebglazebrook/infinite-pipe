# This file is autogenerated by the aliases tool.
# For more info about aliases type `aliases --help`
# or visit https://github.com/sebglazebrook/aliases

release:
  command: ./scripts/release
console:
  command: docker-compose run rust bash
build-debug:
  command: docker-compose run rust cargo build
build-release:
  command: docker-compose run rust cargo build --release
